<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/header') %>

    <div class="container" style="max-width: 600px;">
        <h2 class="mb-4 text-primary">📢 Reporte Ciudadano</h2>
        
        <% if(mensaje) { %>
            <div class="alert alert-success"><%= mensaje %></div>
        <% } %>

        <div class="card p-4">
            <p>Ayúdanos a mejorar. Reporta incidencias en las vías o en los servicios turísticos.</p>
            <form action="/reporte/enviar" method="POST">
                <div class="mb-3">
                    <label class="form-label">Descripción del problema:</label>
                    <textarea name="descripcion" class="form-control" rows="4" required placeholder="Ej: Hay un derrumbe en la subida a El Castrero..."></textarea>
                </div>
                <button type="submit" class="btn btn-danger w-100">Enviar Reporte</button>
            </form>
        </div>
    </div>
</body>
</html>