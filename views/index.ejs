<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guárico TechTur | GTT</title>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@300;400;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    
    <style>
        /* --- NUEVA PALETA DE COLORES --- */
        :root { 
            --primary-blue: #2C5F78; /* Azul Petróleo (Logo) */
            --accent-gold: #D9A54C;  /* Dorado (Morros) */
            --text-black: #333; 
            --text-grey: #666; 
            --bg-white: #fff; 
            --bg-light: #F7F9FB; 
        }
        
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: 'Noto Sans JP', sans-serif; color: var(--text-black); background-color: var(--bg-white); line-height: 1.6; overflow-x: hidden; }
        a { text-decoration: none; color: inherit; transition: 0.3s; }
        ul { list-style: none; }

        /* --- HEADER --- */
        header {
            position: fixed; top: 0; width: 100%; z-index: 1000;
            background: var(--primary-blue);
            box-shadow: 0 4px 12px rgba(44, 95, 120, 0.2);
            padding: 10px 40px;
            display: flex; justify-content: space-between; align-items: center;
            transition: 0.3s;
        }
        
        .logo img { height: 50px; width: auto; display: block; background: white; padding: 2px; border-radius: 5px; }
        
        nav ul { display: flex; gap: 30px; }
        nav a { font-weight: 500; font-size: 0.95rem; letter-spacing: 0.5px; color: white; opacity: 0.9; }
        nav a:hover { color: var(--accent-gold); opacity: 1; }

        /* --- HERO --- */
        .hero {
            height: 90vh; 
            background: linear-gradient(rgba(44, 95, 120, 0.4), rgba(44, 95, 120, 0.6)), url('/images/portada.jpg') no-repeat center center/cover;
            display: flex; align-items: center; justify-content: center; text-align: center; color: white;
            padding: 0 20px;
        }
        .hero-content { max-width: 800px; z-index: 2; }
        .hero h1 { font-size: 4rem; font-weight: 700; margin-bottom: 20px; text-shadow: 2px 2px 10px rgba(0,0,0,0.5); }
        .hero p { font-size: 1.5rem; margin-bottom: 30px; font-weight: 300; }
        
        .btn-japan { background: var(--accent-gold); color: white; padding: 15px 40px; border-radius: 50px; font-weight: bold; border: none; cursor: pointer; box-shadow: 0 4px 15px rgba(217, 165, 76, 0.4); }
        .btn-japan:hover { background: #c59342; transform: translateY(-3px); }

        /* --- GRID --- */
        .section { padding: 80px 40px; max-width: 1400px; margin: 0 auto; }
        .section-header { text-align: center; margin-bottom: 50px; }
        .section-header h2 { font-size: 2.5rem; margin-bottom: 10px; color: var(--primary-blue); }
        .divider { width: 60px; height: 3px; background: var(--accent-gold); margin: 0 auto; }

        .grid-japan {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 30px;
        }

        .card-japan { background: white; border-radius: 12px; overflow: hidden; box-shadow: 0 10px 30px rgba(0,0,0,0.05); display: block; border: 1px solid #eee; }
        .card-japan:hover { transform: translateY(-10px); box-shadow: 0 15px 35px rgba(44, 95, 120, 0.15); }
        .card-img-wrapper { height: 220px; overflow: hidden; }
        .card-img-wrapper img { width: 100%; height: 100%; object-fit: cover; transition: 0.5s; }
        .card-japan:hover img { transform: scale(1.1); }
        .card-content { padding: 25px; }
        .card-title { font-size: 1.3rem; font-weight: 700; margin: 5px 0 10px; color: var(--primary-blue); }
        
        .tag { font-size: 0.75rem; text-transform: uppercase; color: var(--accent-gold); letter-spacing: 1px; font-weight: bold; }
        .status-dot { height: 10px; width: 10px; border-radius: 50%; display: inline-block; margin-right: 5px; }
        .dot-green { background: #4CAF50; box-shadow: 0 0 5px #4CAF50; }
        .dot-yellow { background: #FFC107; box-shadow: 0 0 5px #FFC107; }
        .promo-badge { background: #FFF8E1; color: var(--accent-gold); font-size: 0.7rem; padding: 2px 8px; border-radius: 4px; font-weight: bold; margin-left: 10px; border: 1px solid var(--accent-gold); }

        /* --- FORM --- */
        .report-form { display: flex; gap: 10px; max-width: 500px; margin: 0 auto; }
        .input-japan { flex: 1; padding: 15px; border: 2px solid #eee; border-radius: 50px; outline: none; transition: 0.3s; }
        .input-japan:focus { border-color: var(--primary-blue); }
        .btn-round { width: 50px; height: 50px; border-radius: 50%; background: var(--primary-blue); color: white; border: none; cursor: pointer; transition: 0.3s; }
        .btn-round:hover { background: var(--accent-gold); }

        /* --- FOOTER --- */
        footer { background: var(--primary-blue); color: rgba(255,255,255,0.8); padding: 60px 20px; text-align: center; }
        .authors-list { margin-top: 20px; display: flex; justify-content: center; gap: 15px; flex-wrap: wrap; font-size: 0.9rem; }
        
        @media (max-width: 768px) {
            header { flex-direction: row; padding: 10px 20px; }
            .logo img { height: 40px; }
            nav ul { gap: 15px; font-size: 0.8rem; }
            .hero h1 { font-size: 2.5rem; }
            .hero p { font-size: 1.1rem; }
            .section { padding: 50px 20px; }
            .grid-japan { grid-template-columns: 1fr; }
            .report-form { flex-direction: column; }
            .btn-round { width: 100%; border-radius: 10px; }
        }
    </style>
</head>
<body>

    <header>
        <a href="/" class="logo">
            <img src="/images/logo.jpg" alt="Guárico TechTur">
        </a>
        <nav>
            <ul>
                <li><a href="#destinos">Destinos</a></li>
                <li><a href="#servicios">Servicios</a></li>
                <li><a href="#reporte">Comunidad</a></li>
            </ul>
        </nav>
    </header>

    <section class="hero">
        <div class="hero-content">
            <h1>Descubre Guárico</h1>
            <p>Conecta con la naturaleza y la historia. Información segura y accesible.</p>
            <a href="#destinos" class="btn-japan">Explorar</a>
        </div>
    </section>

    <section id="destinos" class="section">
        <div class="section-header">
            <h2>Destinos Imperdibles</h2>
            <div class="divider"></div>
            <p style="margin-top: 15px;">Estado de rutas en tiempo real</p>
        </div>
        <div class="grid-japan">
            <% atractivos.forEach(lugar => { %>
                <a href="/destinos/<%= lugar.id %>" class="card-japan">
                    <div class="card-img-wrapper">
                        <img src="<%= lugar.imagen %>" alt="<%= lugar.nombre %>">
                    </div>
                    <div class="card-content">
                        <span class="tag"><%= lugar.tipo || 'Turismo' %></span>
                        <h3 class="card-title"><%= lugar.nombre %></h3>
                        <div style="margin-bottom: 10px; font-size: 0.9rem;">
                            <% if(lugar.estado_ruta === 'Habilitada') { %>
                                <span class="status-dot dot-green"></span> Accesible
                            <% } else { %>
                                <span class="status-dot dot-yellow"></span> <%= lugar.estado_ruta %>
                            <% } %>
                        </div>
                    </div>
                </a>
            <% }) %>
        </div>
    </section>

    <section id="servicios" class="section" style="background-color: #F7F9FB;">
        <div class="section-header">
            <h2>Experiencias Locales</h2>
            <div class="divider"></div>
            <p style="margin-top: 15px;">Av. Bolívar y Calle Mellado</p>
        </div>
        <div class="grid-japan">
            <% servicios.forEach(servicio => { %>
                <a href="/servicios/<%= servicio.id %>" class="card-japan">
                    <div class="card-img-wrapper" style="height: 180px;"> 
                        <img src="<%= servicio.imagen %>" alt="<%= servicio.nombre %>">
                    </div>
                    <div class="card-content">
                        <div style="display: flex; justify-content: space-between;">
                            <span class="tag"><%= servicio.categoria %></span>
                            <% if(servicio.promocion) { %> <span class="promo-badge">PROMO</span> <% } %>
                        </div>
                        <h3 class="card-title"><%= servicio.nombre %></h3>
                        <p style="font-size: 0.9rem; color: #666;"><i class="fas fa-clock" style="color: var(--accent-gold);"></i> <%= servicio.horario || 'Abierto hoy' %></p>
                    </div>
                </a>
            <% }) %>
        </div>
    </section>

    <section id="reporte" class="section" style="text-align: center;">
        <div class="section-header">
            <h2>Reporte Ciudadano</h2>
            <p>¿Viste alguna incidencia en la vía?</p>
        </div>
        <form action="/reporte/enviar" method="POST" class="report-form">
            <input type="text" name="descripcion" class="input-japan" placeholder="Ej: Derrumbe en la vía..." required>
            <button type="submit" class="btn-round"><i class="fas fa-paper-plane"></i></button>
        </form>
    </section>

    <footer>
        <div class="logo" style="margin-bottom: 10px;">
            <img src="/images/logo.jpg" alt="GTT Logo" style="height: 40px; border-radius: 4px;">
        </div>
        <div class="authors-list">
            <% autores.forEach(autor => { %> <span><%= autor.nombre %></span> <% }) %>
        </div>
        <br><p>© 2026 UNERG</p>
    </footer>

    <script>
        window.addEventListener('scroll', function() {
            const header = document.querySelector('header');
            header.style.background = window.scrollY > 50 ? 'rgba(44, 95, 120, 0.95)' : '#2C5F78';
            header.style.boxShadow = window.scrollY > 50 ? '0 4px 15px rgba(0,0,0,0.2)' : '0 4px 12px rgba(44, 95, 120, 0.2)';
        });
    </script>
</body>
</html>