<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/header') %>

    <div class="container">
        <h2 class="mb-4 text-primary">🏨 Servicios en Av. Bolívar y Mellado</h2>
        <div class="list-group">
            <% servicios.forEach(servicio => { %>
                <div class="list-group-item list-group-item-action flex-column align-items-start p-4">
                    <div class="d-flex w-100 justify-content-between">
                        <h5 class="mb-1"><%= servicio.nombre %></h5>
                        <small class="text-muted"><%= servicio.categoria %></small>
                    </div>
                    <p class="mb-1">📞 Contacto: <%= servicio.contacto %></p>
                    <% if(servicio.promocion) { %>
                        <div class="alert alert-warning mt-2 mb-0 py-2">
                            <strong>🎁 Promo GTT:</strong> <%= servicio.promocion %>
                        </div>
                    <% } %>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>